{
  "name": "adcock-challenge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "agent": "bun run build && bun dist/agent.js",
    "agent:headed": "bun run build && HEADED=true bun dist/agent.js"
  },
  "dependencies": {
    "@standard-schema/spec": "^1.1.0",
    "@tanstack/ai": "latest",
    "@tanstack/ai-anthropic": "latest",
    "@tanstack/ai-openai": "latest",
    "node-html-markdown": "^2.0.0",
    "playwright": "latest",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
